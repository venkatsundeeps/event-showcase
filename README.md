# Event Agency Showcase — Next.js + Tailwind

A responsive marketing website template for an event management business (weddings, corporate events, parties).

Quick start (Windows PowerShell):

```powershell
npm install
npm run dev
```

Open the URL printed by Next.js (e.g., `http://localhost:3000`).

Notes:

- Uses Next.js App Router and Tailwind CSS.
- UI-only contact form (no backend).
# event-showcase
